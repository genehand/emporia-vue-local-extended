#include "phase_config.h"

// No non-inline implementations required — header contains all inline definitions.
// This file is provided so the build system can compile/link if a .cpp translation unit is expected.
// You can optionally add a translation-unit registration or dummy symbol here if needed.

#if 0
// Example of a dummy symbol to force the object file to be created (uncomment if required):
volatile int phase_config_cpp_dummy = 0;
#endif
